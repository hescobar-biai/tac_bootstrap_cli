#!/usr/bin/env bash
# Lint script for {{ config.project.name }}
# Auto-generated by TAC Bootstrap

set -e

{% if config.commands.lint %}
echo "Running linter for {{ config.project.name }}..."
{{ config.commands.lint }}
{% else %}
echo "No lint command configured"
{% endif %}
