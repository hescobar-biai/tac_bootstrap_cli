#!/usr/bin/env bash
# Build script for {{ config.project.name }}
# Auto-generated by TAC Bootstrap

set -e

{% if config.commands.build %}
echo "Building {{ config.project.name }}..."
{{ config.commands.build }}
{% else %}
echo "No build command configured"
{% endif %}
