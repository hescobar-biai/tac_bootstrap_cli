# List All Available Tools

List ALL available tools (both built-in and MCP tools) available to you at runtime.

Display results in categorized bullet format split into two sections:
1. **Built-in Tools** - Core Claude Code development tools
2. **MCP Tools** - Model Context Protocol tools from configured servers

Use TypeScript function syntax with parameters for each tool.
