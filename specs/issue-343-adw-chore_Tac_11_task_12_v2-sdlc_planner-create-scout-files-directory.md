# Chore: Create scout_files directory template

## Metadata
issue_number: `343`
adw_id: `chore_Tac_11_task_12_v2`
issue_json: `{"number":343,"title":"Create scout_files directory template","body":"chore\n/adw_sdlc_iso\n/adw_id: chore_Tac_11_task_12_v2\n\nCreate the template for scout_files directory in generated projects.\n\n**Files affected:**\n- `/Users/hernandoescobar/Documents/Celes/tac_bootstrap/tac_bootstrap_cli/tac_bootstrap/templates/structure/agents/scout_files/.gitkeep.j2`\n\n**Implementation details:**\n- Create empty .gitkeep.j2 template\n- Ensures scout_files directory is created in generated projec"}`

## Chore Description
Create the template structure for the `scout_files` directory that will be generated in new projects created by TAC Bootstrap CLI. This directory will store files generated by the `/scout` command during codebase exploration. The implementation follows the same pattern as existing agent directories (context_bundles, hook_logs, security_logs).

## Relevant Files
Files needed to complete this chore:

- `tac_bootstrap_cli/tac_bootstrap/templates/structure/agents/security_logs/.gitkeep.j2` - Reference for empty .gitkeep template pattern
- `tac_bootstrap_cli/tac_bootstrap/templates/structure/agents/` - Parent directory for new scout_files subdirectory

### New Files
- `tac_bootstrap_cli/tac_bootstrap/templates/structure/agents/scout_files/.gitkeep.j2` - Empty Jinja2 template to ensure directory creation

## Step by Step Tasks
IMPORTANTE: Ejecutar cada paso en orden.

### Task 1: Create scout_files directory structure
- Navigate to `tac_bootstrap_cli/tac_bootstrap/templates/structure/agents/`
- Create new subdirectory `scout_files/`

### Task 2: Create .gitkeep.j2 template
- Create empty file `scout_files/.gitkeep.j2`
- This follows the same pattern as security_logs, hook_logs, and context_bundles directories
- The empty .gitkeep.j2 ensures the directory is tracked in git and created during project generation

### Task 3: Run validation commands
- Execute all validation commands to ensure zero regressions

## Validation Commands
Ejecutar todos los comandos para validar con cero regresiones:

- `cd tac_bootstrap_cli && uv run pytest tests/ -v --tb=short` - Tests unitarios
- `cd tac_bootstrap_cli && uv run ruff check .` - Linting
- `cd tac_bootstrap_cli && uv run tac-bootstrap --help` - Smoke test

## Notes
- This is a simple directory structure chore that follows existing patterns
- The scout_files directory will store markdown files generated by the /scout command
- No code changes required, only template structure creation
- The .gitkeep.j2 file should be completely empty (0 bytes) to match the pattern of other agent directories
