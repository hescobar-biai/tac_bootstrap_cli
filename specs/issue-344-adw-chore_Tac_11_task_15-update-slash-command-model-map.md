# Chore: Update SLASH_COMMAND_MODEL_MAP in agent.py.j2 template

## Metadata
issue_number: `344`
adw_id: `chore_Tac_11_task_15`
issue_json: `{"number":344,"title":"Update SLASH_COMMAND_MODEL_MAP in agent.py.j2 template","body":"chore\n/adw_sdlc_iso\n/adw_id: chore_Tac_11_task_15\n\nAdd the new TAC-11 slash commands to the model map template.\n\n**Files affected:**\n- `/Users/hernandoescobar/Documents/Celes/tac_bootstrap/tac_bootstrap_cli/tac_bootstrap/templates/adws/adw_modules/agent.py.j2`\n\n**Implementation details:**\n- Mirror changes from Task 14\n- Add `/scout` and `/question` to SLASH_COMMAND_MODEL_MAP\n"}`

## Chore Description
Update the SLASH_COMMAND_MODEL_MAP dictionary in the agent.py.j2 Jinja2 template to include the new TAC-11 slash commands (`/scout` and `/question`). This mirrors the changes already made to the main codebase in Task 14, ensuring that projects generated by the TAC Bootstrap CLI will include these new commands in their model mapping configuration.

The template file is located at:
`tac_bootstrap_cli/tac_bootstrap/templates/adws/adw_modules/agent.py.j2`

## Relevant Files

### Existing Files
- `tac_bootstrap_cli/tac_bootstrap/templates/adws/adw_modules/agent.py.j2` - Jinja2 template for agent.py module. Currently missing `/scout` and `/question` entries in SLASH_COMMAND_MODEL_MAP.
- `adws/adw_modules/agent.py` - Reference implementation that already contains the TAC-11 commands (lines 70-71). This serves as the source for the changes.

### New Files
None required.

## Step by Step Tasks

### Task 1: Read the template file
- Read `tac_bootstrap_cli/tac_bootstrap/templates/adws/adw_modules/agent.py.j2` to locate the SLASH_COMMAND_MODEL_MAP dictionary.
- Identify the exact location where the new entries should be added.

### Task 2: Add TAC-11 commands to SLASH_COMMAND_MODEL_MAP
- Add the following entries to the SLASH_COMMAND_MODEL_MAP dictionary in the template:
  ```python
  # TAC-11: Exploration and clarification (lightweight)
  "/scout": {"base": "sonnet", "heavy": "sonnet"},  # TAC-11: Scout command for exploration
  "/question": {"base": "sonnet", "heavy": "sonnet"},  # TAC-11: Question command for clarification
  ```
- Place these entries at the end of the dictionary, just before the closing brace.
- Ensure proper indentation and formatting matches the existing entries.
- Mirror the exact format from `adws/adw_modules/agent.py` lines 69-71.

### Task 3: Run validation commands
- Execute all validation commands to ensure zero regressions.

## Validation Commands
Execute all commands to validate with zero regressions:

- `cd tac_bootstrap_cli && uv run pytest tests/ -v --tb=short` - Tests unitarios
- `cd tac_bootstrap_cli && uv run ruff check .` - Linting
- `cd tac_bootstrap_cli && uv run tac-bootstrap --help` - Smoke test

## Notes
- This is a straightforward template sync chore.
- The reference implementation already exists in `adws/adw_modules/agent.py`.
- The template uses Jinja2 syntax but this particular section doesn't require any templating variables - it's static Python code.
- Both `/scout` and `/question` are lightweight commands that use "sonnet" for both base and heavy model sets.
